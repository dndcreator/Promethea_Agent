{
  "id": "wecom",
  "kind": "channel",
  "name": "WeCom Channel",
  "description": "Built-in WeCom channel",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": { "type": "boolean" },
      "channel_config": { "type": "object" }
    },
    "additionalProperties": true
  },
  "uiSchema": {
    "fields": [
      { "key": "enabled", "type": "boolean", "label": "Enable WeCom Channel" },
      { "key": "channel_config", "type": "json", "label": "Channel Config (JSON)" }
    ]
  },
  "capabilities": {
    "surfaces": ["wecom"],
    "supports_hot_reload": true
  }
}
